{
  "name": "bitproofs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/sync-z3.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "z3-solver": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "coi-serviceworker": "^0.1.7",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
